# Parchment v0.1.0 — Scope & Audit Tracker

**104 items** across Plans A–L. Single tag `v0.1.0`. This is the source of truth for what shipped.

## Rules

- No item flips to `DONE` until **browser-verified on the live deploy**.
- Every item carries a **Coverage check** and a **Failure-modes-verified** gate before lock.
- If an item does not ship, it is logged `GAP` here with a reason. Never claim done.
- Per-PR artifacts (all five required): spec path · RED-on-main · GREEN-on-branch · live-deploy screenshot · axe-core zero-violations report on affected route.

## Status legend

| Code | Meaning |
|---|---|
| `TODO` | not started |
| `WIP` | in progress |
| `RED` | failing test written, no impl |
| `GREEN` | impl passes tests, not yet browser-verified |
| `DONE` | browser-verified on live deploy + both gates passed + artifacts attached |
| `GAP` | did not ship — reason logged in Notes |

## Gate columns

- **Cov** — Coverage check passed (every sub-behavior in the item has a test).
- **FM** — Failure-modes-verified (error/empty/concurrent/a11y paths exercised).

---

## Plan A — Foundations (5)

| ID | Item | Status | Cov | FM | Notes |
|---|---|---|---|---|---|
| A1 | Scaffold repo, biome, tsconfig, drizzle, **single-container** image (PG18+pgvector+Hocuspocus+Next, s6-overlay) | DONE | ✓ | ✓ | typecheck/lint/build 0; **Vitest+Testcontainers** migration test 3/3; **axe-core 6/6** zero-violations; single-container **browser-verified live** (:3001). a11y contrast bug found+fixed (RED→GREEN) |
| A2 | Auth — PAT + local owner account; OAuth 2.1 + SSO route stubs (v0.2) | DONE | ✓ | ✓ | argon2id + session cookie + Bearer-PAT; /setup + /login; OAuth/SSO 501 stubs; `(app)` gated by requireUser. auth.test green; **browser-verified** owner-creation → session → audit row; axe /login + /setup-form clean |
| A3 | Settings shell — Account / Workspace / Admin / Developer / Notifications / Security | DONE | ✓ | ✓ | 6-group nested-layout shell + active sub-nav; axe 6/6 (fixed inverted active-link contrast, RED→GREEN); browser-verified |
| A4 | Audit log (create/delete/share/export/login) | DONE | ✓ | ✓ | append-only logAudit (never throws) + filterable viewer; Testcontainers test 3/3; **browser-verified** (setup event renders); axe clean |
| A5 | Health page (DB / disk / search index / collab status pills) | DONE | ✓ | ✓ | real probes (db/collab/search-index/disk), resilient; pills page + /api/health; **browser-verified** (db up, collab/disk down shown); axe clean |

## Plan B — Editor core / TIER 1 (14)

| ID | Item | Status | Cov | FM | Notes |
|---|---|---|---|---|---|
| B1 | Page-bounded canvas, `@page` + paged.js, Letter default, A4 toggle, per-section margin+orientation | DONE | ✓ | ✓ | page-width paper + computed break markers + live "Page X · N words" + A4/Letter toggle (816↔794px); paginate unit 15/15; axe clean; browser-verified (3-page overflow, markers, toggle). paged.js print=H2, per-section margins=B14 |
| B2 | Inline formatting bar (B/I/U/S/sub/super/code/highlight/color/font/size/line-height/letter-spacing) | DONE | ✓ | ✓ | fixed top toolbar + selection bubble menu; reactive aria-pressed (useEditorState); onMouseDown keeps selection. marks unit 10/10 (jsdom); axe clean; browser-verified |
| B3 | Block formatting (H1–H6, para, quote, code w/ auto-detect, lists, outlines, align, indent) | DONE | ✓ | ✓ | block-type select (P/H1–6/quote/code), bullet/numbered/task lists, L/C/R/justify align, first-line indent, code-block manual language picker (auto-detect+Shiki=C). blocks unit 13/13; axe clean; browser-verified. Removed dup Underline (StarterKit owns it) |
| B4 | Tables — insert/resize/merge/header/shade/sort, `=SUM/AVG/AVERAGE/COUNT`, ranges `A1:A10` | DONE | ✓ | ✓ | Tiptap Table (resizable) + controls (add/del row-col, merge/split, header toggle, sort, delete); alt-row shade; formula cells via `recomputeFormulas` (Σ btn) using tested `evalFormula`/`expandRange`. Targets the **selected** table (findSelectedTable, unit-proven). 102 unit tests; axe clean; browser-verified (insert, =SUM=30) |
| B5 | Images — paste/drag/upload, position modes, resize, crop, **alt text required**, lock aspect | DONE | ✓ | ✓ | upload→asset route→serve (verified 200 image/png), URL insert, paste/drop, 5 position modes, corner resize + lock-aspect, **alt REQUIRED on insert** (browser-verified: empty alt blocked w/ role=alert). image-node unit 14; axe clean. Canvas-based **crop shipped** (PR #10) — GAP closed. |
| B6 | Links — auto-detect, named, link-to-heading, link-to-doc (fuzzy picker) | DONE | ✓ | ✓ | StarterKit Link configured in-place (autolink+linkOnPaste, rel=noopener), 3-mode LinkPopover (URL/heading/doc); heading anchor ids (HeadingId global attr — **fixed: was not rendering id**); `/api/docs/search` fuzzy picker. 144 unit+integration tests; axe clean; browser-verified (named link href+rel, h2 id=slug, search 200) |
| B7 | Auto TOC — `/toc`, refresh, optional page numbers + leader dots | DONE | ✓ | ✓ | `toc` NodeView reading `collectHeadings`; auto-refreshes on editor updates + manual Refresh; nested anchor links to `#id`; page-numbers toggle → leader dots + page (headingPage from B1 paginate). 156 tests; axe clean; browser-verified (entry→#introduction-section, dots on toggle). Toolbar insert btn (slash insert=B12) |
| B8 | Footnotes + endnotes — `[^1]`, numbered, click-jump, footer/end per-section | DONE | ✓ | ✓ | `[^…]` input rule + toolbar btn → superscript ref + end-of-doc item; auto-numbered (appendTransaction); bidirectional click-jump (ref `#fn-def-id` ↔ definition back-link ↩ `#fnref-id`); markdown `[^N]`/`[^N]:`. 169 tests; axe clean; browser-verified (2 refs numbered, anchors both ways). **Note: footer placement = print-time (H2); on-screen both render end-of-doc** |
| B9 | Find + replace — case/word/regex, replace all, scope, ⌘F / ⌘⇧H | DONE | ✓ | ✓ | pure `findMatches`/`applyReplacements` (case/word/regex, invalid-regex→error not throw) mapped to PM decorations; panel w/ counter, next/prev, replace+replace-all, doc/selection scope; ⌘F/⌘⇧H keymap. 188 tests; axe clean; browser-verified (3 matches "1 of 3", replace-all→THE×3, invalid regex role=alert) |
| B10 | Word + char count — live, selection-scoped, reading time | DONE | ✓ | ✓ | pure `countText`/`readingTimeMinutes`; StatusBar live doc counts + reading time (238wpm), switches to selection counts when text selected (useEditorState). 197 tests; axe clean; browser-verified ("5 words·23 chars·1 min read"; sel "2 words·7 chars") |
| B11 | Outline pane — collapsible rail, jump, drag-reorder subtree | DONE | ✓ | ✓ | left rail reads `collectHeadings` (live), collapsible pane + per-heading subtree collapse, click/Enter jump to `#id`, drag-reorder moves whole section (heading + descendants) via `moveSection`/`moveHeadingSection` (collect-then-adjust PM positions). 216 tests (19 reorder, subtree integrity); axe clean; browser-verified (lists heading, jump). |
| B12 | Slash menu — `/`, categories BASIC/TEXT/LISTS/MEDIA/EMBED/ADVANCED, category rail | DONE | ✓ | ✓ | `@tiptap/suggestion` `/` trigger → ReactRenderer popup; left category rail (All/BASIC/TEXT/LISTS/MEDIA/EMBED/ADVANCED); `filterSlashItems` live filter; arrow/Enter/Esc keyboard; insert removes `/query` then runs command (Image→opens dialog). 234 tests; axe clean; browser-verified (/head→H1/2/3, Enter inserts H1 + clears query) |
| B13 | Page primitives — page numbers, running headers/footers per-section, `/pagebreak`, section breaks | DONE | ✓ | ✓ | `pageBreak`+`sectionBreak` nodes (toolbar + slash ADVANCED); PageCanvas merges manual+auto breaks (`mergeBreaks`) → boundary overlays w/ page numbers (`formatPageNumber` 1/i/I/a/A) + running header/footer per resolved section. 272 tests; axe clean; browser-verified (break→boundary+page# "1"). **Section-break edit dialog SHIPPED** (NodeView "Edit section" btn → `parchment:edit-section` event → SectionBreakDialog → `setNodeMarkup` at node pos; browser-verified label→"Chapter One"). Remaining: page-1 footer + true per-page = print H2 |
| B14 | Margins + page setup dialog — in/cm, custom margins, orientation, Letter/A4/Legal/Tabloid/Custom | DONE | ✓ | ✓ | PageSetupDialog: size (Letter/A4/Legal/Tabloid/Custom W×H), orientation, in/cm unit toggle, 4 custom margins → `PageSetup`/`resolvePageDims` applied live to PageCanvas. 281 tests (conversions+resolve); axe clean; browser-verified (Letter→A4 resizes 816→794px). **GAP: per-section size/orientation deferred (doc-level full; section attrs don't carry page setup yet)** |

## Plan C — Code block (7)

| ID | Item | Status | Cov | FM | Notes |
|---|---|---|---|---|---|
| C1 | Code block UI — language picker + auto-detect option | DONE | ✓ | ✓ | toolbar code-lang `<select>` gains "Auto-detect" (sentinel `__auto__`); reads active block text, runs detectLanguage, sets `language` attr → Shiki recolors. browser-verified (Python→picker shows Python). axe clean |
| C2 | Auto-detect — `highlight.js/lib/core` + `auto`, low-confidence → plaintext | DONE | ✓ | ✓ | `detectLanguage` via hljs/lib/core + 19 registered langs → `normalizeLang`; relevance<5 or <12 chars → plaintext; never throws. auto-detect unit 8/8 (python/go/ts real snippets); browser-verified (13 colored spans after detect) |
| C3 | Shiki render — 6 bundled themes, default + per-block override | DONE | ✓ | ✓ | Shiki 4.2 singleton (6 themes), highlights via ProseMirror **decorations** (code stays editable); per-block `theme` attr (default github-light). 311 tests; axe clean; browser-verified (TS → 20 colored spans, 6 token colors) |
| C4 | Top-50 languages, lazy-load grammars by name | DONE | ✓ | ✓ | `TOP_LANGUAGES` (50) + `normalizeLang` aliases + `isSupportedLanguage`; grammars lazy-loaded on demand (`ensureLanguage`), unknown→plaintext, async re-decorate via `shikiReady` meta. shiki-languages unit 28/28; browser-verified (typescript grammar loaded on use) |
| C5 | Line numbers (per-block toggle), line highlight `{1,3-5}`, filename caption | DONE | ✓ | ✓ | CodeBlockView NodeView header: line-num toggle (widget-decoration gutter), highlight-lines input (`parseLineRanges`), filename caption. code-block-lines unit 15/15; browser-verified (line #, filename "patch.diff") |
| C6 | Copy button + collapse on hover | DONE | ✓ | ✓ | header Copy (clipboard + transient ✓) + Collapse/Expand (`collapsed` attr, aria-expanded) buttons; keyboard-reachable. browser-verified (buttons present in header). axe clean |
| C7 | Diff highlighting for `diff` language | DONE | ✓ | ✓ | `diffLineKind` → inline +green/−red line decorations when language='diff' (`+++`/`---` excluded). picker now lists all 50 langs incl Diff (was missing — fixed). browser-verified (green +line). |

## Plan D — Collab / review (5)

| ID | Item | Status | Cov | FM | Notes |
|---|---|---|---|---|---|
| D1 | Comment threads — anchor, replies, resolve, @-mention, filter open/resolved/mine | DONE | ✓ | ✓ | `comments` table (migration 0002) + comments-repo + API (`/api/docs/[id]/comments`); `comment` mark anchors selection (click→`parchment:focus-comment`); CommentsSidebar (threads/replies/resolve/3-way filter), `parseMentions` (client-safe shared module — split repo to fix pg-in-client). comments unit+integration; axe clean; browser-verified (add→anchored+@alice, reply, resolve→mark removed+under Resolved) |
| D2 | Suggesting mode — tracked insert/delete/format, accept/reject, accept-all, side-by-side, author colors | DONE | ✓ | ✓ | Suggesting toggle; `insertion`/`deletion` marks (author color), insert via appendTransaction, delete via handleKeyDown (Backspace/Del/selection); SuggestionsPanel accept/reject per change + accept/reject-all (`resolveChange`/`collectChanges`). track-changes 14 + suggesting 9 (jsdom). browser-verified (insert tracked, reject removes/accept keeps text). **GAPs: format-change tracking, true side-by-side (inline marks=v0.1 view), IME/cut/paste-over-selection** |
| D3 | Version history — autosave 30s + named snapshots, visual + unified-md diff, restore | DONE | ✓ | ✓ | `doc_versions` (migration 0003) + versions-repo + API (list/snapshot/restore); 30s autosave snapshot (only if markdown changed) + named snapshots; `diffMarkdown`/`unifiedPatch` (jsdiff) A/B compare; restore is reversible (pre-restore snapshot). version-diff unit 10 + versions integration 7; axe clean; browser-verified (v1/v2 + auto snapshot, diff old+new, restore→reverted) |
| D4 | Real-time multi-cursor + presence (Yjs/Hocuspocus) | DONE | ✓ | ✓ | `HocuspocusProvider` binds the editor Y.Doc to collab server (`ws://localhost:1234`, `NEXT_PUBLIC_COLLAB_URL`); `CollaborationCaret` renders remote carets (name from `currentUserName`, color from `authorColor(currentUserId)`). **First-open seeding is gated on a server-rendered `hasCollabState` prop (NOT a racy onSynced fragment-length check)** — fixes content duplication when seeding raced authoritative server state. Offline path (onClose / 4s timeout / construction fail) force-seeds the mirrored `documents.content` then `disconnect()`s so a reconnect can't merge-duplicate; timer cleared on unmount. **Persistence fix: `collab_state.state` text→bytea (migration 0004) + `Buffer.from(state)` bind** — binary Yjs updates now persist (was failing "invalid byte sequence"). 393 unit+integration (incl. 0004 migration); axe clean (doc route). Browser-verified on prod build: no duplication on first-open OR reload-authoritative, state persists (bytea, 0 encoding errors), editor+typing work with collab DOWN, real-time bidirectional sync + remote caret rendering between two live clients. |
| D5 | Collaborative reading position | DONE | ✓ | ✓ | Built on D4 awareness. Each client publishes `user` (on mount, always-on presence) + a throttled `reading` `{pos,updatedAt}` (doc pos at centre of the editor's **visible band**, clamped to viewport — naive `vh/2` probe sat above short docs and published nothing; fixed). `ReadingPresence` overlay renders per-remote-reader gutter avatars (initial + user color) at their `coordsAtPos`, click-to-jump (`domAtPos`→element `scrollIntoView`); StatusBar shows `👁 N reading` pill (aria-label lists names). Pure `reading-presence.ts` (`collectReaders` filter/stale/sort + leading/trailing `throttle`) — 11 unit tests. axe clean (doc route). Browser-verified prod build, 2-client iframe: **bidirectional** markers + pill, click-jump no-throw. **GAP: marker only shows while peer connected (no last-seen persistence); single-doc presence only (no cross-doc "who's where").** |

## Plan E — File manager / TIER 4 (11)

| ID | Item | Status | Cov | FM | Notes |
|---|---|---|---|---|---|
| E1 | Folders + nested, drag-drop reparent | DONE | ✓ | ✓ | `folders-repo` (create/list/rename/move/delete — all owner-scoped; `moveFolder` cycle-guarded, `deleteFolder` transactionally reparents child folders+docs to grandparent) + `listDocumentsInFolder`/`moveDocument` in repo. Pure `folder-tree.ts` (`buildTree`/`folderPath`/`wouldCreateCycle`, orphan+cycle-safe) — 16 unit + 7 integration (testcontainers). API: `/api/folders` (GET/POST), `/api/folders/[id]` (PATCH rename/move→409 on cycle, DELETE), `/api/docs/[id]/move`, `/api/docs?folder=`. `/files` now a client `FileManager`: nested folder tree, breadcrumb nav, native HTML5 drag-drop reparent (doc→folder, folder→folder, →root). axe clean. Browser-verified prod build: create+nest folders, navigate, drag doc into folder (root 23→22, present in folder), folder→child blocked (409 alert), folder→root valid. |
| E2 | Recents / Starred / Shared (stub) / Trash views | DONE | ✓ | ✓ | `documents.starred` bool (migration 0005); repo `listRecents`/`listStarred`/`listTrashed` (→`DocRow` w/ starred) + `setStarred`/`trashDocument`/`restoreDocument` (owner-scoped). API: `/api/docs/[id]/{star,trash,restore}` + `/api/docs?view=recents|starred|trash`. FileManager 5-tab switcher (`nav aria-label=views`, `aria-current`): All=E1 folder browse; Recents/Starred/Trash=flat lists w/ ★ toggle + 🗑 trash + Restore; Shared=v0.2 stub. 5 integration tests. axe clean. Browser-verified prod build: star→Starred (1 doc), trash (Recents 23→22)→Trash→Restore (→0), Shared stub. **GAP: E11 owns trash retention + empty-now; "shared" is a stub (single-owner v0.1).** |
| E3 | Smart folders — live saved searches | DONE | ✓ | ✓ | `smart_folders` table (id/ownerId/name/criteria jsonb, migration 0006). Pure `smart-folder-criteria.ts` (`parseCriteria` sanitizes unknown JSON, `describeCriteria` label) — 12 unit. `smart-folders-repo` CRUD (owner-scoped) + `runSmartFolder` builds live drizzle `and()` from criteria (titleContains ilike / starred / folderId, always non-trashed) → DocRow newest-first — 12 integration. API: `/api/smart-folders` (GET/POST), `/[id]` (PATCH/DELETE), `/[id]/results` (GET live run). FileManager: left-rail Smart Folders list + inline labelled create-form (name / title-contains / starred-only checkbox) + ✕ delete; `view='smart'` shows criteria description + live FlatDocRow results. axe clean. Browser-verified prod build: create "title contains Untitled"→23 live results, delete→removed. **GAP: criteria limited to title/starred/folder (tag criteria awaits E4, full-text awaits E9).** |
| E4 | Tags — color-coded, picker, filter, bulk tag | DONE | ✓ | ✓ | `tags` + `document_tags` (m2m, composite PK, cascade) tables (migration 0007). Pure `tag-colors.ts` (8 AA-contrast palette colors, `resolveTagColor`/`isValidTagColor`) — unit tested. `tags-repo` CRUD + addTagToDoc(idempotent)/removeTagFromDoc/listTagsForDoc/listDocsForTag/tagCounts (owner-scoped) — integration tested. API: `/api/tags` (GET+counts/POST), `/[id]` (PATCH/DELETE), `/[id]/results`, `/api/docs/[id]/tags` (GET/POST/DELETE). FileManager: left-rail Tags section (color dot + name + live count + ✕ delete) + create form (name + color select); `view='tag'` filter results; per-doc 🏷 popover (checkbox assign) + inline color chips; count badge refreshes live after assign/remove (controller fix). 407 unit + 14 integration. axe clean. Browser-verified prod build: create tag(red)→assign(DB-confirmed, count→1)→filter(exactly tagged doc)→remove(→0)→live badge refresh. **GAP: bulk-tag deferred to E6 (needs bulk-select); sidebar only shown in All/smart/tag views (flat views are full-width).** |
| E5 | Sort + view toggle — name/modified/created/size, grid/list/details + thumbnail | DONE | ✓ | ✓ | `DocRow` extended w/ `createdAt`+`size` (`length(markdown)`)+`preview` (`left(markdown,140)`) computed in SQL (no migration). Pure `doc-sort.ts` `sortDocs` (name/modified/created/size, asc/desc, stable) — 9 unit. FileManager: sort `<select>` + asc/desc toggle, 3-way view-mode toggle (List/Grid/Details, `aria-pressed`, localStorage-persisted); unified `DocList` renderer — List=rows, Grid=cards w/ text-preview thumbnail, Details=`<table>` w/ clickable `<th aria-sort>` headers; sort applied across all doc lists (folder/recents/starred/trash/tag/smart). Size shown human-formatted ("5.5k"). 417 unit + 12 integration. axe clean. Browser-verified prod build: List/Grid/Details render (23 cards w/ previews, sortable table), Size sort asc[0,3,5,5,6]↔desc[5500,85,83,76]. **GAP: real rendered-page image thumbnails out of scope (text preview used).** |
| E6 | Bulk select — shift/⌘/drag-region, bulk move/tag/delete/export | DONE | ✓ | ✓ | Pure `selection.ts` (`rangeBetween` inclusive shift-range, `toggle` immutable Set) — 11 unit. `POST /api/docs/bulk` (`{ids,action:move\|trash\|tag,folderId?,tagId?}`, per-id ownership check skips cross-owner, returns `affected`) — 7 integration incl. cross-owner-untouched. FileManager: per-row checkboxes (List/Grid/Details) + select-all + shift-click range + `BulkActionBar` (`section aria-label=Bulk actions`: Move-to folder select, Add-tag select, 🗑 Delete, Clear); selection clears on view/folder switch. **Closes E4's bulk-tag GAP.** axe clean. Browser-verified prod build: shift-range(4 inclusive), bulk-delete(Recents 23→19, Trash 4), bulk-tag(3 docs DB-confirmed), bulk-move(2 docs→folder DB-confirmed). **GAP: bulk-export→H8 (ZIP); marquee/drag-region selection out of scope.** |
| E7 | Right-click menu — rename/move/duplicate/template/star/share/export/open/show/trash | DONE | ✓ | ✓ | `renameDocument` + `duplicateDocument` (owner-scoped) repo + `/api/docs/[id]/rename` + `/duplicate`. Pure `context-actions.ts` `docMenuItems` (9 items, star label flips, template/share disabled-w-note) — unit tested. FileManager `ContextMenu` (`role=menu`, Esc/outside-close): Open, Rename (prompt→persist), Duplicate ("(copy)"), Star/Unstar, Export-as-Markdown (client blob download from stored markdown), Show-in-folder (navigateTo + All view), Move-to-Trash; **Save-as-template (→Plan G) + Share (→v0.2) disabled placeholders**. Opened via keyboard-accessible ⋯ "Actions for {title}" button (all views) + right-click on List-view rows. 433 unit + 10 integration (rename/duplicate incl. owner-scoping). axe clean. Browser-verified prod build: ⋯ menu (9 items), rename, duplicate, export (`text/markdown` blob, `{title}.md`), right-click(list). **GAP: right-click only on List rows (Grid/Details use ⋯ button); export-as-Markdown only (docx/pdf=Plan H).** |
| E8 | Breadcrumbs — click-jump + drag-drop reparent on hover | DONE | ✓ | ✓ | E1 shipped click-jump breadcrumbs (Root / A / B, each navigates). E8 wraps every crumb (incl. Root) in the existing `DropZone` → dragging a doc/folder onto a crumb reparents it into that folder, with an over-highlight; reuses `handleDrop` (folder cycles still 409 server-side). Controller inline change. biome/tsc/build 0, 433 unit. axe clean. Browser-verified prod build: in Root/E8Folder/E8Sub, dragged a doc onto the "E8Folder" crumb → doc moved to E8Folder (DB-confirmed), left the E8Sub view. |
| E9 | Search — tsvector FTS + pgvector semantic, filters, ⌘K hybrid toggle | DONE | ✓ | ✓ | **FTS:** `search_vector` made a GENERATED tsvector col over title+markdown (migration 0008, gin index kept); `searchFullText` (`websearch_to_tsquery` + `ts_rank`, folder/tag/starred filters, non-trashed). **Semantic: PLUGGABLE, OFF by default** (user decision) — `embeddings.ts` (`isSemanticEnabled`=`!!EMBEDDINGS_URL`, `embed()` OpenAI-compatible fetch, 768-dim, never throws); `searchSemantic` (pgvector `<=>` cosine); best-effort embed-on-save (never blocks). `/api/search?q=&mode=keyword\|semantic&folder=&tag=&starred=` (graceful semantic→FTS fallback when disabled). **⌘K palette** (`CommandPalette`, mounted in app layout): Cmd/Ctrl+K modal, keyword/semantic toggle (semantic disabled+"(not configured)" when no endpoint), debounced live results, ↑↓/Enter/Esc. 441 unit (8 embeddings) + 14 integration (11 search incl. semantic-nearest via seeded embeddings). axe clean. Browser-verified prod build: ⌘K opens, keyword "content"→2 results (matches API+DB), semantic correctly disabled, Esc closes. **GAP: semantic dormant without `EMBEDDINGS_URL` (verified via integration, not live); no bundled model (self-host opt-in).** |
| E10 | ⌘P fuzzy file finder | DONE | ✓ | ✓ | Pure `fuzzy.ts` (`fuzzyScore` fzf-style subsequence + consecutive/word-start bonuses, `fuzzyFilter` ranked) — 14 unit. `/api/docs/titles` (lightweight id+title list). `FileFinder.tsx` (Cmd/Ctrl+P, `preventDefault` to block browser print; `role=dialog aria-label="Go to file"`; client-side fuzzy over all titles; ↑↓/Enter/Esc) mounted alongside ⌘K in `CommandPaletteMount`. 455 unit. axe clean. Browser-verified prod build: ⌘P opens, "projrd"→"Project Roadmap" (fuzzy subsequence), Enter→opens that doc. |
| E11 | Trash retention — configurable, "empty now" gated by typed confirm | DONE | ✓ | ✓ | `settings` table (owner+key+jsonb, composite PK; migration 0009) + `settings-repo` (`getSetting`/`setSetting` upsert, `getTrashRetentionDays` default 30). `purgeExpiredTrash` (hard-deletes trashed-owned docs older than N days, no-op if ≤0) wired LAZILY into `/api/docs?view=trash`; `emptyTrash` (all trashed-owned). API `/api/settings/trash-retention` (GET/PUT clamp≥0) + `/api/trash/empty` (**server-side phrase re-validation `'empty trash'` → 400 else**). FileManager Trash toolbar: retention number input (PUT on blur) + Empty-Trash button → typed-confirm dialog (`role=dialog`, Confirm disabled until phrase matches). 455 unit + 15 integration (purge/empty/owner-scoped). axe clean. Browser-verified prod build: retention=30 shown, empty-trash dialog (disabled→wrong-phrase-disabled→correct-enables→trash 2→0, **DB hard-deleted**), API rejects bad confirm (400), retention PUT persists. **Controller fix: removed drizzle's spurious `search_vector DROP EXPRESSION` from 0009 (would've broken E9 FTS).** **GAP: scheduled purge → Plan I10 (lazy purge on trash-load for now).** |

## Plan F — Disk mirror / TIER 3 (6)

| ID | Item | Status | Cov | FM | Notes |
|---|---|---|---|---|---|
| F1 | Every doc → real `.md` under `~/parchment/files/<folder>/<file>.md`, configurable root | DONE | ✓ | ✓ | **Write-side mirror** (F2=chokidar reverse). `documents.disk_path` col (migration 0010). Pure `disk/paths.ts` (`sanitizeSegment`, `docRelPath` nests folder chain, `disambiguate` ` (2)` on collision, case-insensitive) — 15 unit. `disk/mirror.ts` (`filesRoot()` reads `PARCHMENT_FILES_ROOT` at call time; `syncDocToDisk` builds path→disambiguate vs other docs' disk_path→mkdir-p→write markdown→relocate old + prune empty dirs→update disk_path; `removeDocFromDisk`) — queries `@/db` directly (no repo import → no cycle); **all fs ops best-effort, never throw**. Hooked into save/move/rename/restore/duplicate (sync) + trash (remove) in repo.ts. 474 unit + 7 integration (real mkdtemp dir). Root configurable via `PARCHMENT_FILES_ROOT` (container `/data/files`). Browser-verified prod build (root `/tmp/parchment-dev/files`): rename→`DiskMirrorTest.md` written w/ markdown; rename→relocates (old gone); folder-move→`MirrorFolder/DiskMirrorRenamed.md`; disk_path tracks. **GAP: reverse sync (disk→doc) + conflict = F2; per-doc git = F4.** |
| F2b | Disk↔collab live bridge — external edits reach the Y.Doc (closes F3-discovered gap; user-approved add) | DONE | ✓ | ✓ | The watcher + Y.Doc bridge MOVED into the collab server (editor graph crashes the Next turbopack runtime but loads under collab's tsx). On reverse-sync `apply`, an injected hook (`setApplyToYDoc`, kept editor-graph-free in reverse-sync.ts) calls `server.hocuspocus.openDirectConnection(docId).transact(ydoc => { pmNode.check() BEFORE mutating — Yjs transact has no rollback; updateYFragment(ydoc, frag, pmNode, {mapping,isOMark}) })` → Hocuspocus broadcasts to open clients + persists collab_state. Next instrumentation watcher removed (no double-watch). Dockerfile ships `src/`+tsconfig to the collab service. Built via Workflow (implement→3-lens review: loop-termination lens proved the cross-process loop **terminates via serializer-fixpoint two-write settle** not byte-identity, caught a half-mutation risk + a too-weak test → both fixed). 501 unit (ydoc-apply replace-not-append) + 8 integration. Browser-verified prod build: **external `.md` edit → OPEN editor live-updates with no reload (content replaced not appended); loop settles (file hash stable, no spam, no errors)**. **GAP/behavior: hand-editing a `.md` in non-canonical markdown gets reformatted to canonical on next autosave (one extra write, convergent); a few constructs (hard breaks, indented code, nested-list spacing) lossy on round-trip.** |
| F2 | chokidar watcher — external edits sync back, conflict detect | DONE | ✓ | ✓ | **Reverse side** of disk mirror. `documents.disk_synced_hash` baseline (sha256 of last-synced markdown; migration 0011) drives a pure `sync-decision.ts` `classifyChange`→echo/apply/conflict (echo checked first → loop provably terminates). `reverse-sync.ts handleExternalChange`: resolve doc by disk_path, hash file+db, classify; **apply** = parse md→JSON + direct UPDATE guarded by **optimistic CAS** on disk_synced_hash (no TOCTOU clobber of concurrent in-app edits — adversarial-review fix); **conflict** = write `<name>.conflict-<ms>.md` (deduped, excluded from watch) + preserve both sides (zero data loss). `markdown/parse.ts` = hand-rolled `marked`-token→PM-JSON (**no editor graph / @tiptap/html — those crash the Next server runtime**; controller fix after live boot 500). chokidar watcher in `instrumentation.ts` (nodejs-runtime guard, try/caught so it never crashes boot). Deps: chokidar@5, marked@18. Built via orchestrated workflow (implement→3-lens adversarial review caught the CRITICAL TOCTOU data-loss race→fix). 610 unit+integration (sync-decision/markdown-parse unit + reverse-sync 5 integ). Browser-verified prod build: watcher boots, external .md edit→doc syncs (md+content JSON), forced conflict→1 `.conflict` file + doc preserved, no loop/spam, unlink graceful. **GAP: custom blocks (page/section breaks, footnotes, TOC, formula tables) not reverse-parsed→F3; unlink doesn't delete doc; new .md doesn't create doc (import=H9).** |
| F3 | Markdown canonical form — lossless, extension blocks as fenced `parchment:*` | DONE | ✓ | ✓ | Custom nodes round-trip via fenced `parchment:<kind>` (single-line JSON body): **pageBreak**→`parchment:pagebreak`, **sectionBreak**→`parchment:section` (headerText/footerText/pageNumberFormat/pageNumberPosition, default-backfilled symmetrically — review fix), **toc**→`parchment:toc` (showPageNumbers, default-backfilled), **table**→`parchment:table` (full node JSON — formula/colspan/rowspan/colwidth all preserved; all tables fence since serialize has no GFM-pipe output). Footnotes stay GFM `[^N]`. serialize.ts + parse.ts stay **editor-graph-free** (server-runtime-safe). Malformed fence → degrades to plain code block, never throws. Built via Workflow (implement→2-lens review caught toc default-backfill asymmetry + weak test→fixed). 9 round-trip unit tests (deep-equal) + 499 unit + 7 F2 integration. Browser-verified prod build: external fenced md→reverse-sync reconstructs sectionBreak(attrs)+pageBreak in **server runtime**; editor save re-emits `parchment:table` fences. **(disk-mirror↔collab gap CLOSED by F2b — external `.md` edits now reach the open editor live + collab_state.) Plain tables emit as JSON fences not human-readable GFM.** |
| F4 | Per-doc git via isomorphic-git — autocommit, log, cherry-pick, branch, merge | DONE | ✓ | ✓ | One git repo at `filesRoot`; the **collab watcher is the single committer** (fires on every `.md` change — forward mirror writes AND external edits → no cross-process index race). `git/repo.ts` (isomorphic-git@1.38.5, all ops best-effort/serialized via one promise queue): `ensureRepo`, `commitPath` (empty-commit-guarded via statusMatrix), `removeAndCommit` (phantom-delete-guarded — atomic-rename saves don't churn; controller fix), `logForPath` (`git.log({filepath})` → per-doc commits), `readAtCommit` (`readBlob` → content at oid), `createBranch`/`mergeBranch` (lib + tested). API `/api/docs/[id]/git-log` + `/git-show?oid=` (ownership-checked). UI: git commits surface in the 🕐 Version-history panel. Watcher reuses reverse-sync's `relPathIfManaged` (no classifier drift — controller fix). Built via Workflow (implement→2-lens concurrency/correctness review: all-minor, confirmed serialization + best-effort + per-doc log scoping). 501 unit + 20 integration (12 git-repo). axe clean. Browser-verified prod build: rename+2 content edits→**3 commits for GitTest.md** via git-log; git-show oldest oid=historical content ≠ newest 'VERSION TWO'; history panel renders commits. **GAP: cherry-pick=not native (satisfied by readAtCommit+restore); branch/merge are lib-only (no UI); git-log capped 100 commits; restore-from-git-commit UI deferred (reuse D3 restore).** |
| F5 | Plain-text unified diff alongside visual diff (ties D3) | DONE | ✓ | ✓ | Reuses D3's `unifiedPatch` (jsdiff `createTwoFilesPatch`) + new pure `parseUnifiedHunks` (classifies add/del/hunk/meta/context — unit-tested). VersionHistory panel gains a **Visual/Unified** `aria-pressed` toggle: version A/B compare renders either the visual `DiffLine[]` or the unified patch (AA-contrast +/- coloring); a selected **git commit** (F4) also shows a unified diff **vs the canonical current markdown** (`serializeMarkdown(editor.getJSON())` — controller fix; was plain-text `getText()`). 507 unit. axe clean. Browser-verified prod build: toggle switches modes; git-commit-vs-current unified diff renders proper `@@ -1,3 +1,3 @@` hunks with +/- lines (typed marker appears as `+`); matching commit→empty diff (correct). |
| F6 | `[[doc-name]]` wiki backlinks + autocomplete + backlinks panel | DONE | ✓ | ✓ | `wikiLink` atom node (renders/links to `/d/<targetId>`, label sanitized of `[`/`]` so it round-trips — review fix) + `[[` autocomplete (`@tiptap/suggestion`, **distinct PluginKey** — controller fix: default key collided w/ slash-menu → crashed every editor) querying `/api/docs/search`. `doc_links` table (migration 0012, cascade FKs); pure `extractTargetIds` (walks PM JSON) + `doc-links-repo` `setDocLinks` (**transactional** replace — review fix) / `backlinks` (owner-scoped, excludes trashed); extraction hooked best-effort into `saveDocument`. serialize→`[[Label]]`, parse `[[Label]]`→wikiLink (server-graph-free, sync). `/api/docs/[id]/backlinks` + 🔙 BacklinksPanel. Built via Workflow (2-lens review caught non-atomic setDocLinks + label-bracket corruption→fixed). 525 unit + 19 integration. axe clean. Browser-verified prod build: editor mounts, `[[Backlink`→autocomplete shows BacklinkTarget→inserts `[[BacklinkTarget]]`→`doc_links` row→backlinks API+panel link the source. **GAP: hand-typed/external `[[Label]]` markdown parses with `targetId=''` (sync server parse can't title-resolve) — unresolved until relinked in-editor.** |

## Plan G — Tiers 2–8 (17)

| ID | Item | Status | Cov | FM | Notes |
|---|---|---|---|---|---|
| G1 | Sharing — link share (view/comment/edit/suggest), per-email (stub), password, expiry, anyone-toggle | DONE | ✓ | ✓ | `shares` table (token 256-bit base64url, argon2id passwordHash, expiresAt; migration 0013, cascade FKs). `shares-repo` (owner-scoped CRUD; `resolveShare` server-side expiry; `verifySharePassword` argon2; pure `isExpired`). API `/api/docs/[id]/shares` (GET safe-shape `hasPassword` not hash / POST), `/api/shares/[id]` DELETE, public `/api/share/[token]` (resolve+password→content; 401 required/wrong; 404 invalid/expired/**trashed**). Public `/share/[token]` OUTSIDE auth gate; `ShareViewer` = static PM-JSON→React renderer (**no editor, no dangerouslySetInnerHTML, no contenteditable**; restricted hrefs → structurally read-only). Share dialog (perm/password/expiry/copy/revoke; per-email v0.2 stub). Built via Workflow (2 security lenses→caught trashed-doc leak, fixed: trash revokes shares). 533 unit + 14 integration. axe clean. Browser-verified prod build: plain→read-only render; password required/wrong(401)/correct(200); expired/revoke/trash→404; no hash leak; token 43-char. **GAP: write perms stored but render read-only (anon writes=v0.2); no password-attempt rate-limit.** |
| G2 | Templates — bundled gallery + save-as-template | DONE | ✓ | ✓ | `templates` table (owner+name+desc+content jsonb; migration 0014). Pure `builtin-templates.ts` (5: blank/meeting-notes/letter/weekly-report/project-brief — valid PM JSON) unit-tested. `templates-repo` (createTemplate/createTemplateFromDoc owner-scoped/listTemplates/deleteTemplate/getTemplateContent). API `/api/templates` (GET/POST {name,fromDocId?} — **content validated as PM `doc` or 400**, review fix), `/[id]` DELETE, `/api/docs/from-template` {builtinKey?\|templateId?}→new doc (404 unknown/empty). `/templates` gallery (Bundled + Your-templates sections, Use→instantiate→open, delete). Save-as-template in FileManager E7 context menu. Built via Workflow (correctness review→caught `{}` invalid-content crash, fixed). 537 unit + 10 integration. axe clean. Browser-verified prod build: gallery shows 5 bundled + user template; instantiate meeting-notes→new "Meeting notes" doc renders full structure; save-as-template→appears in gallery; unknown/empty→404. |
| G3 | Styles system — named para/char styles, inherit chain, workspace CSS theme, accent, font pairs | DONE | ✓ | ✓ | Reuses `settings` store (no migration): keys `workspaceTheme` + `docStyles`. Pure `theme.ts` (`FONT_PAIRS` 5, `parseTheme` validates hex/known pair, `themeCssVars`→`--accent`+`--accent-contrast`+`--font-heading`+`--font-body`; review fix: emit **both** accent tokens — UI uses bare `--accent` in ~40 places too) — injected on the app-shell wrapper in layout.tsx. Pure `styles.ts` (`NamedStyle`, `resolveStyleProps` cycle-safe basedOn chain child-wins, `parseStyles`, `DEFAULT_STYLES` 4). API `/api/settings/theme` + `/api/settings/styles` (GET/PUT). Workspace Appearance section (accent color + font-pair). Editor `StylesMenu` dropdown applies resolved props via chain commands. Built via Workflow (review→caught partial-accent-application, fixed). 552 unit (theme+styles). axe clean. Browser-verified prod build: accent #ff1493→both CSS vars live + serif font pair; Styles dropdown lists custom+inherited; apply BigRed(basedOn BaseRed)+type→text gets color #cc0000(parent)+24pt+bold(child) — inherit chain resolves. **GAP: paragraph-vs-character styles apply inline-only (no block-range expansion); styles manager add/delete only (no inline edit); new styles show on next editor mount.** |
| G4 | Equation editor — KaTeX inline/display, numbering, eq refs | DONE | ☑ | ☑ | Slash Equation/Inline/Ref; MathPopover live KaTeX preview→Apply persists latex (data-latex set, .katex renders in editor), auto-number (1); schema builds server-side (no katex leak in collab/parse/serialize), $5.00 stays text. 565 unit (6 numbering+7 roundtrip). MINOR: numberMathBlocks JSON-branch position-key off (live path uses PMNode branch, correct). |
| G5 | Drawing — Excalidraw embed, SVG out, editable on reopen | DONE | ☑ | ☑ | excalidraw@0.18.1 dynamic(ssr:false) — never on server path (build clean, collab schema restart clean, getSchema prints ['drawing'] w/o loading it). Slash Drawing → auto-opens modal (controller-fixed: insertDrawing pure, slash handler resolves node-selected pos AFTER .run()); draw→Done persists scene+svg to correct node (no dup), <img src=data:svg renders (XSS-safe, no dangerouslySetInnerHTML); editable on reopen (re-seeds saved scene); reload-persistent; lossless parchment:drawing disk-mirror md (scene+svg). 570 unit. Workflow review caught the stale-pos data-loss (fixed aa01190); controller live-verify caught auto-open gap (fixed 159d09c). |
| G6 | Diagrams — Mermaid + PlantUML + Drawio, live preview | DONE | ☑ | ☑ | All 3 engines, schema server-safe (getSchema prints drawing/mermaid/plantuml/drawio w/o loading mermaid/excalidraw; collab tsx boots clean). MERMAID local: slash→auto-open popover→live preview→Apply→`<img data:svg>` renders real diagram, reload-persists. PLANTUML pluggable (NEXT_PUBLIC_PLANTUML_SERVER_URL, OFF by default per E9): off→disabled note + source, NO external call; on→`<img>` from configured /svg/ server loads (verified www.plantuml.com 237×151). DRAWIO pluggable (NEXT_PUBLIC_DRAWIO_EMBED_URL, OFF by default): off→disabled note, no iframe; on→embed.diagrams.net iframe loads (embed=1&proto=json); read-render stored svg→`<img data:svg>`. Lossless md: ```mermaid / ```plantuml normal fences, ```parchment:drawio fence {xml,svg} — all round-trip in DB markdown. 600 unit. Workflow per-engine review caught 7 blocking (4-backtick fence data-loss ×2, drawio postMessage origin spoof, etc); cross-review caught + fixed pre-existing math stale-pos. **GAP: Drawio interactive embed→save round-trip not browser-driven (origin-checked external app, impractical headless; postMessage save wiring code-reviewed + iframe verified). MINOR: PlantumlView/render-pm static import plantuml.ts (bundle-size only); parseDrawioExport atob no Node fallback (browser-only path); updateDrawio stale if collab moves node mid-modal (v0.1 single-user).** |
| G7 | Citations — DOI via CrossRef, CSL (APA/MLA/Chicago), bibliography block, cite-by-key | DONE | ☑ | ☑ | DEVIATION: "Cairn lib" not on this machine → hand-rolled PURE CSL formatter (src/lib/citations/{types,format,crossref}.ts — server-safe, offline, no heavy dep; clean iface to swap a real engine later). Bibliography block (refs+style, parchment:bibliography fence) + inline citation ([@key] Pandoc form) + resolution plugin (mirror math numbering). Browser-verified prod: in-text resolves "(Smith & Jones, 2020)" and RE-RESOLVES live across APA/MLA/Chicago (both in-text + bib reformat); DOI add via real CrossRef call (Watson-Crick 1953 fetched→CSL→added); [@key]+parchment:bibliography round-trip in DB md (refs+style preserved). Formatting spot-checked all 3 styles incl n.d. 637 unit. **2 BUGS caught by live-verify (gate green, missed them): (1) resolution-plugin INFINITE LOOP — apply rebuilt Map on repaint-meta→view re-dispatched→loop pegged renderer (fixed: rebuild only on docChanged, mirror math); (2) CitationView not reactive to style change (fixed: useEditorState). Both controller-fixed.** GAP: @-autocomplete UI not browser-driven (synthetic input can't trigger @tiptap/suggestion; established pattern, distinct PluginKey, insertCitation works). MINOR: APA full given-name not initials, MLA URL terminal period, non-'p.' locator round-trip, footnoteRef [^N] pre-existing parse gap. |
| G8 | Cross-references — figure/table/eq/heading, auto-update on move | DONE | ☑ | ☑ | Stable refId (auto-assigned UUID, identity) + dynamic per-kind number (doc order). Figures (image+caption), tables (caption), equations (mathBlock), headings (B6 id). Numbering plugin (mirror math, rebuild only on docChanged — no G7 loop) + crossRef inline node, REACTIVE via useEditorState. Browser-verified prod: crossRef "Figure 2" → AUTO-UPDATES to "Figure 1" on figure reorder (headline ✓); captions show "Figure N: caption"; crossRef clickable (goto-ref); [#refId] + parchment:figure fence (caption+refId) round-trip in md; heading id survives md via `<!-- id:slug -->` sentinel. 661 unit (+ PMNode regression test). **BUG caught by live-verify (gate green, missed it): collectCrossRefTargets read node.type as STRING (`=== 'image'`) — true only for JSON docs; real PMNode type is a NodeType OBJECT → empty numbering map → every ref "(?)". Fixed: normalize type name; added jsdom regression test that builds a real Editor.** GAP: all images now serialize as parchment:figure fences (every image gets a refId) — plain images lose standard `![](...)` markdown form (lossless but less portable). MINOR: caption "Figure N" prefix only for images w/ refId (all get one). |
| G9 | Watermark — text/image, per-doc or per-section | DONE | ☑ | ☑ | Text/image overlay, opacity/rotation/tile/color/fontSize. Per-doc in documents.meta.watermark (migration 0015 — see below); per-section override via SectionConfig.watermark resolved per page. WatermarkDialog (≋ toolbar) → PUT /api/docs/[id]/watermark (owner-scoped, 404 if not owned, merges meta). PageCanvas renders N per-page overlays (one per page region, overflow:hidden — fixes rotation bleed + multi-page PRINT; single full-canvas overlay wouldn't replicate per printed page), pointer-events:none, aria-hidden, zIndex 0. Browser-verified prod: dialog all fields, "CONFIDENTIAL" renders 2 per-page layers, persists to meta + loads on reload, typed through overlay (editing not blocked). 683 unit. Workflow review fixed 4 blocking (false-200→404, rotation bleed, per-section parseWatermark bypass, multi-page print). **BUG caught by live-verify (server-breaking, gate green): brief WRONGLY said documents.meta existed — implementer added it to schema.ts w/o migration → every /d/[id] query failed "column meta does not exist" (ERROR 905014507). Fixed: migration 0015 ADD COLUMN meta jsonb, applied dev+e2e, migration.test green.** GAP: image-watermark imageUrl protocol not allowlisted (minor, flagged); print multi-page structurally fixed but not browser-print-tested (headless). |
| G10 | Voice typing — Web Speech API into selection | DONE | ☑ | ☑ | Mic button (always renders, reads SpeechRecognition fresh at click → graceful + testable). voice.ts pure (isVoiceSupported, getSpeechRecognition, formatTranscript: spoken punctuation "period"→".", "new line"→"\n", sentence-start capitalize, join spacing). VoiceButton: start→continuous+interim, onresult final→insertContent at cursor, interim preview (aria-live), recording aria-pressed. Browser-verified prod (fake SpeechRecognition injected): mic renders, start sets aria-pressed, final result "hello world"→inserts "Hello world" (capitalized); stop→aria-pressed false; unsupported→graceful "not supported" message, no crash, no start. 708 unit (25 voice). Workflow review fixed 7 blocking (all SpeechRecognition lifecycle races: dangling rec on double-click, onend after stop/abort, stale-session onend wiping new ref, onresult after unmount, conditional aria-live). GAP: real speech-to-text (browser↔STT service) not headless-testable — standard Web Speech API, full button→onresult→insert wiring verified w/ fake recognition; race-condition fixes code-reviewed not race-tested. |
| G11 | PWA / offline — SW cache, offline edit, sync on reconnect | DONE | ☑ | ☑ | Manifest (app/manifest.ts, name+3 icons 192/512/maskable), hand-rolled SW (public/sw.js: network-first nav, cache-first hashed /_next/static, network-only /api+WS+non-GET, versioned cache, skipWaiting+claim), prod-only registration, y-indexeddb on Y.Doc (offline persistence), offline indicator. Browser-verified prod: manifest valid (name+icons), SW registers+active, content loads ONCE no duplication (collab+IDB CRDT-merge across reloads), provider syncs to collab, editor interactive through SW, offline indicator shows "⊘ Offline" when collab down. 716 unit (8 SW cache-rule). Workflow review fixed 11 blocking (SW addAll atomicity, skipWaiting await, networkFirst redirect-cache, +the y-indexeddb seed-vs-IDB-sync DUPLICATION race [the D4 footgun #3 — idbSyncedRef written-but-never-read]). **2 MORE BUGS caught by live-verify: (1) offline COLD-SEED broken — a doc-with-collab-state showed EMPTY when collab server unreachable (IDB-synced force=false call pre-empted goOffline's force-seed via seededRef; AND goOffline-before-IDB-sync lost the force intent across the deferral). Fixed both: don't mark seeded in hasCollabState branch + latch force across IDB deferral. (2) collab server can't reach DB locally — `pnpm collab` doesn't load .env → DATABASE_URL undefined → falls back to :5432 (taken) → auth fail → collab offline (dev-setup gap, NOT G11/prod; Docker sets env). Flagged for fix.** GAP: true network-offline edit→reconnect→sync not headless-tested (can't toggle network); components verified individually (IDB persist + provider reconnect + indicator). |
| G12 | Mobile responsive editor — touch toolbar, page-fit, swipe pages | DONE | ☑ | ☑ | page-fit.ts pure (pageFitScale clamp≤1, isMobileWidth@768, classifySwipe). usePageFit hook: measures canvas-wrap offsetWidth, gates isMobileWidth, sets --page-scale on .parchment-canvas-scaled-host; .parchment-page transform:scale(var) + height:calc(natural*scale) compensation. Touch toolbar @media≤768 (overflow-x:auto, 44px targets). Swipe gated isMobileWidth(innerWidth). viewport width=device-width+initialScale=1 (pinch-zoom preserved). 733 unit. Workflow review fixed 14 BLOCKING (RO feedback-loop collapsing page metrics, scaled-offsetHeight reads, swipe-using-total-scrollHeight, swipe-not-mobile-gated, calc(auto*scale)). Browser-verified prod: DESKTOP unchanged (transform none @1280px); page-fit RENDERS correctly when var set (--page-scale:0.4→matrix(0.4), visual width 326px fits, host height compensates 516px no gap); hook ACTIVELY manages var by container width (reverts manual override → confirms live measurement); NO RO collapse loop (heights stable 1291×3); touch toolbar overflow-x:auto on mobile @media. GAP: preview reports innerWidth=1312 + canvas-container 804px even at mobile preset (preview can't produce a <768px JS-measured container/DPR quirk) → AUTOMATIC scale-trigger + swipe at a real phone width not browser-verified; the JS-measure→CSS-render pipeline verified in two halves + unit-tested. |
| G13 | AI compose sleeve — Ollama/Anthropic/OpenAI, improve/shorten/translate/continue → suggesting mode | DONE | ☑ | ☑ | Pluggable OpenAI-compatible endpoint (AI_BASE_URL, OFF by default per E9; works w/ Ollama /v1, OpenAI). ai/prompts.ts pure (buildMessages per op), ai/compose.ts SERVER-ONLY (reads AI_API_KEY, never client; isAiEnabled, composeText caps 8000ch, strips fences, never throws), /api/ai/compose auth-gated (503 disabled, 502 fail, 200 {result}), AiMenu bubble-menu "✦ AI" (Improve/Shorten/Translate/Continue), aiEnabled server-prop. Browser-verified prod (local OpenAI-compat stub): OFF default → no AI button + route 503 ai_disabled; ON (AI_BASE_URL set) → "✦ AI" appears, route 200, Improve → result lands as TRACKED suggestion — new text INSERTION mark + old text DELETION mark (struck, reviewable, NOT auto-applied), suggesting state restored (finally). Key server-side (route proxies; client fetches route; compose.ts only RSC-imported). 750 unit. Workflow security review fixed 8 BLOCKING — CRITICAL: replace ops were AUTO-COMMITTED (suggesting plugin only tracks net-positive insertions + keyboard deletes, not programmatic replaces) → fixed by manually adding deletion mark to [from,to] then inserting at from; stale-selection-after-async-fetch → re-read/clamp; suggesting-stuck-on → finally restore. GAP: real LLM (Ollama/OpenAI) not used (verified w/ stub; standard OpenAI-compat shape); Anthropic native API not impl (OpenAI-compat endpoint only — Anthropic has one). |
| G14 | Smart paste — content-type sniffer → Word/GDocs/Notion/web/markdown normalisers | DONE | ☑ | ☑ | smart-paste.ts pure (sniffPasteSource by markers, looksLikeMarkdown, normalizePastedHtml DOMParser-based per source), SmartPasteExtension (transformPastedHTML + markdown handlePaste). Browser-verified prod: WORD → mso/o:p/MsoNormal/StartFragment stripped, content+heading+bold kept (`<h1>Quarterly Report</h1><p>Revenue grew <b>15 percent</b></p>`); GDOCS inline font-weight:700 span→`<strong>` + guid wrapper unwrapped; WEB `<script>`/onclick stripped (XSS-safe) + link kept; INTERNAL PM copy-paste UNCHANGED (data-pm-slice→plain, textStyle/color marks preserved); markdown plain-text → heading+list+bold nodes. 786 unit (36 smart-paste jsdom). Workflow review fixed 6 BLOCKING (internal-paste classified 'web' stripping color/font marks → data-pm-slice→plain; image-paste guard cd.files→also cd.items; markdown false-positives). **BUG caught by live-verify (gate green, all normalizers shadowed): smart-paste declared transformPastedHTML as a ProseMirror PLUGIN PROP, but Tiptap COMPOSES every extension's transformPastedHTML into one EDITOR-LEVEL fn (extensionManager.transformPastedHTML) that someProp checks BEFORE plugin props — the composed identity e=>e shadowed the plugin → NO normalization ran live despite pure-fn unit tests passing. Fixed: declare it as the Tiptap extension-LEVEL field. Also hardened createTreeWalker/createTextNode to the parsed doc.** |
| G15 | Reading mode — full-bleed, sepia/serif/wide-margin, per-doc bookmark | DONE | ☑ | ☑ | reading.ts pure (ReadingPrefs sepia/serif/wide, readingClassNames, parse, ls key builders). ReadingView full-bleed [role=dialog aria-modal] overlay reusing renderReadOnlyDoc (read-only, no editor), 3 independent theme toggles + Close, prefs in localStorage (global), scroll bookmark per-docId. Browser-verified prod: 📖 toggle → overlay w/ read-only content (no contenteditable); Sepia→warm bg rgb(42,35,24), Serif→Georgia, Wide→608px narrow column (wide margins), all combinable + aria-pressed; scroll to 800px → saved ls[parchment:reading-bookmark:<docId>]=800 → restored on reopen; Close returns to editor. 802 unit (16 reading). Workflow review fixed 6 BLOCKING (suggestion-menu z-index 9999 bleeding through overlay z-200→raised; no focus trap; focus not returned on close; +minor negative-bookmark guard, double-rAF scroll restore, scroll container tabIndex). |
| G16 | Presenter mode — F5 page-flip, arrows, speaker notes | DONE | ☑ | ☑ | presenter.ts pure splitIntoSlides (split top-level pageBreak, extract speakerNote→slide.notes, ≥1 slide, no input mutation). speakerNote node (parchment:speakernote fence, render-pm→null so never leaks to share/reading). PresenterView full-bleed [role=dialog] overlay: slide stage renderReadOnlyDoc(slide.content) + notes strip (UNWRAPS speakerNote inline content — can't reuse renderReadOnlyDoc which suppresses it) + counter, ←→/Space/PageDn nav clamped, Esc exits, F5 keydown (browser-intercept caveat + toolbar ▶ fallback). 811 unit (8 presenter). Workflow review fixed 9 BLOCKING — CRITICAL: notes panel BLANK (reused renderReadOnlyDoc → speakerNote null-suppressed in its OWN render path) → unwrap inline content; F5-close-reopens (child+parent double window keydown listener → net toggle stays open) → guard Editor handler !presenterOpen; Space/Backspace preventDefault too broad. Browser-verified prod: ▶→overlay, slide 1 body (not slide 2), counter 1/2, note in notes panel NOT slide body; →slide 2 (2/2), ←back, Esc exits; speakerNote does NOT leak to reading mode (render-pm null). GAP: F5 key capture browser-dependent (button is the verified path). |
| G17 | Custom CSS per doc | DONE | ☑ | ☑ | custom-css.ts pure: scopeCustomCss (prefix every selector with .parchment-custom-scope, @media/@supports inner-prefix, @keyframes/@font-face left, fallback '' on parse oddity) + sanitizeCustomCss (strip @import, @scope block, external/protocol-rel/js/data url()→url(''), expression(), '<', length cap + defense-in-depth catch-alls for url()/expression edge cases) + prepareCustomCss + parseCustomCss. Stored RAW in documents.meta.customCss (sanitize+scope at render); setDocumentCustomCss merges (preserves watermark), PUT /api/docs/[id]/custom-css owner-scoped. CustomCssStyle <style> in scoped wrapper, applied editor+reading+share. Browser-verified prod: dialog → apply `h1{color}` + `.parchment-toolbar{display:none}` → h1 turns green BUT toolbar UNAFFECTED (scoped — chrome unreachable); persists meta across reload. 841 unit (30 custom-css). Workflow security review fixed CRITICAL @scope escape (`@scope(:root){body{}}` bypassed prefix defense — prelude evaluated vs whole doc → stripped @scope entirely) + controller hardened url()/expression sanitizer edge cases (quoted-`)`, unclosed url, expression>500). |

## Plan H — Export / import / TIER 5 (9)

| ID | Item | Status | Cov | FM | Notes |
|---|---|---|---|---|---|
| H1 | `.docx` round-trip via Mammoth | DONE | ☑ | ☑ | EXPORT via docx@9.7.1 (docToDocx → Uint8Array). Browser-verified: export?format=docx → valid ZIP (PK magic) + word/document.xml, correct content-type, 8.9KB. Round-trip IMPORT side = H9 (Mammoth). Headings/lists/tables/data-images mapped. |
| H2 | `.pdf` via paged.js (page fidelity) | DONE | ☑ | ☑ | Export menu "PDF" → PrintView full-bleed overlay (portal to body for @media print), renderReadOnlyDoc + injected @page style (pageCss from PageSetup: size/margins) → window.print → browser save-as-PDF. paged.js best-effort preview (dynamic import ssr:false). Browser-verified prod: PDF button → overlay opens, @page CSS injected, content visible, Print fires window.print. Workflow review fixed 6 BLOCKING (print @media body> selectors→PORTAL to body; paged.js inline-CSS-as-URL→object form; Previewer style leak→destroy; focus-in-aria-hidden; print-btn-on-error). **CONTROLLER fixes: (1) pnpm allowBuilds placeholder strings for pagedjs deps core-js/es5-ext broke `next build` dep-check→set false; (2) paged.js Previewer throws under Turbopack ("s.call is not a function")→graceful NATIVE-print fallback (content in-flow + @page CSS drives browser pagination = correct PDF). GAP: paged.js precise in-app preview fails in bundle (native browser pagination used); actual print-to-PDF dialog not headless-testable.** |
| H3 | `.html` standalone, embedded CSS, no JS | DONE | ☑ | ☑ | docToStandaloneHtml = renderToStaticMarkup(renderReadOnlyDoc) wrapped in <!doctype> + embedded EXPORT_STYLESHEET, NO <script>/external (plantuml stripped to <pre> source). Browser-verified: GET /export?format=html → standalone, content-type text/html, download filename, content rendered. |
| H4 | `.md` canonical lossless round-trip | DONE | ☑ | ☑ | reuses serializeMarkdown (F3 lossless). Browser-verified: export?format=md → # heading, **bold**, - list; text/markdown; download filename. |
| H5 | `.epub` long-form | DONE | ☑ | ☑ | docToEpub via jszip@3.10.1 — valid EPUB3 ZIP (mimetype FIRST + STORE, META-INF/container.xml, OEBPS/content.opf manifest+spine, nav.xhtml, chapter.xhtml from H3 body). Browser-verified: export?format=epub → application/epub+zip, mimetype-first, container+opf present. |
| H6 | LaTeX — equation + bibliography preserved | DONE | ☑ | ☑ | docToLatex pure (article class, headings→section, marks, lists, math inline/display, citations→cite + thebibliography). Browser-verified: section{Report 50%% & Done} (specials escaped), href{raw url} (not escaped), inline code escaped, math present. Workflow review fixed 11 BLOCKING: sout w/o ulem→added; href URL over-escaped→raw; inline-code under-escaped→full escapeTex; verbatim end-injection→hardened. |
| H7 | plain `.txt` | DONE | ☑ | ☑ | docToPlainText walk (headings/paras→lines, lists→prefixed, tables→tab, marks stripped). Browser-verified: export?format=txt → plain (no md syntax), text/plain. |
| H8 | Bulk export — multi-select → ZIP in chosen format | DONE | ☑ | ☑ | buildBulkZip (per-doc exportDoc + unique filenames + skip-on-fail) via jszip; POST /api/export/bulk {ids,format} owner-scoped (skips non-owned, id cap), FileManager BulkActionBar "Export as ZIP" format select → blob download. Browser-verified prod: POST 2 real+1 fake id, md → application/zip "parchment-export.zip" (PK), 2 entries Bulk-Doc-A.md/Bulk-Doc-B.md w/ content, FAKE id skipped/not leaked, empty ids→400. 891 unit (5 bulk). Clean review (0 findings). |
| H9 | Import — docx/md/html/Notion-zip/Google-Docs-paste | DONE | ☑ | ☑ | src/lib/import/index.ts: detectImportType + everything→Markdown→markdownToJson (md direct, docx via mammoth→html→turndown+jsdom→md, html via turndown, notion-zip via jszip). POST /api/docs/import (runtime=nodejs, auth, multipart, content-length+413 cap, zip-bomb + path-traversal guards, 415 unknown, createDocument try/catch). FileManager Import button. GDocs-paste = existing G14 smart-paste. Browser-verified prod: md import→content (heading/bold/list); html import→turndown md; DOCX ROUND-TRIP (export docx→import mammoth) preserves heading/**bold**/- list (H1 acceptance MET); MALFORMED docx→200 partial + warning "docx conversion failed", NO crash. Build compiles (jsdom/mammoth server-safe). 910 unit (19 import). Workflow review fixed 6 BLOCKING (createDocument→500, zip-bomb uncompressed cap, zip path-traversal, post-buffer size guard→content-length, html error-swallow). |

## Plan I — Settings / admin / ops / TIER 6 (10)

| ID | Item | Status | Cov | FM | Notes |
|---|---|---|---|---|---|
| I1 | Theme — light/dark/system, accent picker, page bg, font-pair gallery | DONE | ☑ | ☑ | Extended G3 WorkspaceTheme + colorScheme(light/dark/system) + pageBg(white/sepia/hex); parseTheme legacy-compat, themeCssVars emits --page-bg, ACCENT_SWATCHES(8)+PAGE_BG_PRESETS. globals.css scheme refactor ([data-color-scheme] forces over @media prefers; propagate to :root for html/body bg). AppearanceSettings: scheme radios + 8 swatches + hex + page-bg + font gallery. Browser-verified prod (preview colorScheme emulation): FORCED-LIGHT on OS-dark → body rgb(247,246,243) LIGHT (crux bug fixed); FORCED-DARK on OS-light → body rgb(22,21,26) DARK; accent #0099ff + sepia page-bg #f5efe0 apply + persist across reload. 932 unit. Workflow review fixed 6 BLOCKING (forced-scheme @media :root bleed → data-color-scheme propagate; code-block pre :root selector; page-bg onBlur preset-clobber). |
| I2 | Keyboard shortcuts — ⌘⇧/ cheat sheet, customizable, Vim source mode | DONE | ☑ | ☑ | 3 sub-features. (1) ⌘⇧/ global chord opens I9 cheat sheet via GlobalShortcuts dispatcher. (2) Customizable APP-LEVEL shortcuts: pure keymap.ts (canonicalKey + code-based matchesCombo, splitCombo tokenizer, mergeBindings/findConflicts), keymap-repo + GET/PUT /api/settings/shortcuts, central dispatcher honors overrides (counted handler registry → only preventDefaults registered actions), ShortcutsSettings remap UI (record/save/reset + aria-live). Editor formatting marks (⌘B/I) read-only ref rows (GAP: not remappable v0.1). (3) Vim source mode: SourceMode.tsx lazy-loads CodeMirror6+@replit/codemirror-vim (own async chunk, 0 static refs), serialize.ts→edit→markdownToJson round-trip, collab-safety (open gate collabActive + live ydoc.update/awareness watcher + exit re-snapshot + window.confirm before overwrite). **Browser-verified live (preview, single-user):** ⌘⇧/ (dispatched key='?'/code='Slash' shift+meta)→cheat sheet opens (defaultPrevented, shows ⌘⇧/ as / not ?); remap UI shows 4 customizable combos+Save+aria-live; source mode→CodeMirror+vim renders, doc serialized to markdown, "editing canonical Markdown" lossy warning shown, Done→content round-trips back to ProseMirror (no spurious confirm); collab-gate disables source mode when awareness>1 peer present. 1014 unit (+ new keymap tests for ⌘⇧/ code:Slash + Mod+-/+ binding), pnpm build compiles (lazy CodeMirror chunk). Review found 11 (8 BLOCKING) — first fix agent emitted garbage; 2nd fix (eccc309) resolved all: A ⌘⇧/ never fired (key '?'≠'/' → code:Slash match), E can't bind -/+ (split[+-]→splitCombo), B presenter can't close (toggle + remove PresenterView hardcoded F5), C F5 hijacked app-wide (registry: only preventDefault registered actions), D source-mode Y.Doc clobber (watcher+re-snapshot+confirm); minors autosave-pause-in-source + recorder aria-live. GAPs: QWERTY assumption in key-fold (code-path layout-immune); window.confirm unstyled; footnoteRef no md round-trip (pre-existing parse.ts); stale Hocuspocus ghost awareness can disable source mode for a solo user until collab server clears. |
| I3 | Autosave cadence slider 5s–5min | DONE | ☑ | ☑ | autosave-constants.ts (client-safe clamp 5s–5min, split from settings-repo to avoid @/db in client bundle), getAutosaveInterval/set + PUT/GET /api/settings/autosave, AutosaveSlider in workspace settings, Editor reads autosaveIntervalMs prop (clampAutosaveMs defensive, re-creates interval on change). Browser-verified: PUT 60000→60000, 1000→5000 (MIN), 9999999→300000 (MAX). 943 unit. Review fixed CRITICAL @/db client-import build-break (split constants) + NaN clamp + Promise.all→allSettled. |
| I4 | Backup — workspace .zip, scheduled S3 (Cairn CFG-2 pattern), restore | DONE | ☑ | ☑ | backup/archive.ts LOSSLESS (manifest.json + docs/<id>.json raw PM JSON + best-effort .md never-read-back), H9 zip guards (100MB/50MB caps + safeEntryName) reused for restore; service.ts restore resilient (per-doc try/catch, folderId dropped if foreign) + createDocument fix (re-derive markdown via serializeMarkdown + syncDocToDisk at chokepoint → restored/imported docs searchable + disk-mirrored). s3.ts isS3Configured (all 4 BACKUP_S3_* env) + uploadToS3 dynamic-imports @aws-sdk/client-s3 (own externalized server chunk, absent from .next/static; forcePathStyle for MinIO/R2). scheduler s3-backup job registered ONLY when configured (off-unless-configured / CFG-2 — E9). Routes export/restore/s3-now (s3-now requireAdmin). Admin backup page + BackupControls island. **Browser-verified prod build (:3210) incl REAL MinIO round-trip (live-S3 GAP CLOSED, not just mock):** no-S3→jobs[trash-purge,db-heartbeat] (no s3); export 200 zip(manifest v1+raw json+md); restore created 22/skipped 0; restored rich doc (drawing+mathInline+citation) content DEEP-EQUALS original byte-for-byte; restored doc markdown re-derived (# heading + $E=mc^2$); WITH S3 env→s3-backup job appears; s3-now→runCount 1/ok/82ms + 2 real 35KiB .zip objects landed in MinIO bucket. 981 unit, pnpm build compiles. Review fixed 1 BLOCKING (createDocument didn't set markdown/disk → restored docs body-unsearchable + no mirror). MINOR: no bucket-side retention (operator lifecycle); restore additive (re-restore duplicates — by design, surfaced in UI copy); jobs re-run on boot (extra backup on restart — harmless). |
| I5 | Audit log (= A4) | DONE | ☑ | ☑ | Single impl = A4 (append-only logAudit, filterable viewer, testcontainers + browser-verified, axe clean). No separate work — shared. |
| I6 | Health page (= A5) + Ollama + S3 pills | DONE | ☑ | ☑ | A5 base + probeOllama (configured on AI_BASE_URL, GET /models, resilient down) + probeS3 (configured on BACKUP_S3_ENDPOINT) — configured-only (null+omit when unset), probeAll Promise.allSettled (one failure never blanks page). Browser-verified: unconfigured→pills [db,collab,search-index,disk] NO ollama/s3; AI_BASE_URL set→ollama pill appears (down/resilient), all pills still render. |
| I7 | MFA + passkeys (reuse Cairn lib) | DONE | ☑ | ☑ | Built on custom argon2id session auth (no Lucia). mfa.ts PURE (generateTotpSecret, totpUri, verifyTotpStep RFC-6238 ±1 window returning matched step, 80-bit CSPRNG recovery codes Crockford-base32, constant-time compare). Migrations 0016 (user_mfa{totp_secret,totp_enabled_at,recovery_codes argon2-hashed}, passkeys{credID,publicKey,counter}, sessions.mfa_pending) + 0017 (sessions.failed_mfa_attempts, user_mfa.last_totp_step) APPLIED :5433+:5434. Routes: mfa/totp/init|enable|disable, mfa/verify, passkey/register|auth options+verify (server crypto otpauth/qrcode/@simplewebauthn/server dynamic-imported; @simplewebauthn/browser client-only). LOGIN GATE: login action creates createPendingSession(mfa_pending=true) when userHasSecondFactor; validateSession resolves ONLY mfa_pending=false (pending≠logged in); mfa/verify promotes via promotePendingSession (deletes pending row + inserts fresh-token full session = rotation). Brute-force bounded 3 ways: per-IP rate limit + hard per-pending-session attempt cap (failed_mfa_attempts, burns session at 5) + TOTP replay guard (last_totp_step watermark). WebAuthn RP/origin fixed server config (fails closed in prod). Security settings page (TOTP enroll QR+once-shown recovery codes, passkey register/list/remove). **Browser/HTTP-verified prod build (:3210):** init 200 (uri+10 recovery+QR); enable 200 → DB totp_enabled_at set + secret stored + recovery argon2-HASHED; GATE pending session→/api/schedules 401; verify valid TOTP→200 + token ROTATED + promoted session 200; replay pending→401; invalid code→400. 1035 unit, build compiles. Review fixed 6 BLOCKING (no rate-limit→limiter+session cap; header-derived RP/origin→fixed/fail-closed; TOTP replay→last_totp_step; 32→80-bit recovery; token not rotated→promotePendingSession fresh token; ALSO disable/remove not audit-logged). GAPs: login-gate + passkey ceremonies not E2E-tested (need seeded cookie-jar + real/virtual authenticator — TOTP gate IS HTTP-verified above); passkey removal no fresh-reauth; in-process rate limiter resets on restart (DB session-cap is the authoritative bound). |
| I8 | SSO / SCIM route stubs (v0.2) | DONE | ☑ | ☑ | /api/auth/sso (501 sso_not_available) + /api/scim/v2/Users + /Groups (501 SCIM-shaped error body, schemas array). v0.2 scaffold. Browser-verified: sso 501, scim 501 + SCIM-shaped. |
| I9 | Help menu — replay tour / shortcuts / what's new drawer | DONE | ☑ | ☑ | help/content.ts (SHORTCUTS, RELEASE_NOTES v0.1.0, TOUR_STEPS), HelpMenu in sidebar footer → Keyboard shortcuts dialog + What's new drawer + multi-step Replay tour (auto-once via parchment:tour-seen localStorage, replayable). Browser-verified: Help menu → [Keyboard shortcuts, What's new, Replay tour]; shortcuts dialog shows ⌘K/slash/etc. 952 unit. Review fixed 4 BLOCKING a11y (Esc not writing tour-seen→re-shows; focus-restore unreachable isOpen=true; role=menu without arrow-nav). |
| I10 | Schedules — in-process scheduler **on-by-default, NO env flag** (avoid Cairn CFG-3) | DONE | ☑ | ☑ | jobs.ts PURE timer-free tick(nowMs) core (per-job in-flight lock → no pile-up/re-entry) + scheduler.ts singleton (idempotent start, globalThis HMR cache, unref'd 60s setInterval). Default jobs trash-purge(24h, per-owner resilient via getTrashRetentionDays) + db-heartbeat(5min). instrumentation.register() boots scheduler.start() UNCONDITIONALLY (NEXT_RUNTIME nodejs guard + try/catch + dynamic import; no editor graph; **NO env flag** — Cairn CFG-3 avoided). GET/POST /api/schedules admin-gated (new requireAdmin, owner role); admin/schedules page + RunNowButton island. Browser-verified prod build (:3210, ZERO config): GET /api/schedules→[trash-purge,db-heartbeat] on-by-default; run-now db-heartbeat→runCount 1/status ok/dur 1ms (wall-clock fix proven); unknown job→404; no-cookie→401; admin page 200 renders jobs+Run now. 962 unit. Review fixed 4 BLOCKING (tick pile-up/re-entry→in-flight lock+per-tick guard; runNow race; lastDurationMs injected-vs-wall clock; cross-tenant trash-purge→requireAdmin). MINOR logged: in-memory state resets on restart (job re-runs on boot — idempotent, OK v0.1); scheduler.ts @/db lacks server-only import (comment only). |

## Plan J — Integrations / TIER 7 (7)

| ID | Item | Status | Cov | FM | Notes |
|---|---|---|---|---|---|
| J1 | Cairn cross-link — `[[cairn://page-id]]`, preview card, bidirectional | DONE | ☑ | ☑ | Mirrors F6 wiki-link for external Cairn pages. cairnLink node {pageId,label} (cairn-link.ts, getSchema-safe) + cairn-suggestion.ts (DISTINCT PluginKey). integrations/cairn.ts: isCairnEnabled=!!CAIRN_BASE_URL, cairnPageUrl (null when unset), fetchCairnPagePreview (resilient null when disabled), isValidCairnPageId/sanitizeCairnPageId. cairn_links table (migration 0018, PK source_doc_id+page_id, FK documents cascade) APPLIED both DBs; setCairnLinks at saveDocument chokepoint (repo.ts:89, extractCairnPageIds). md round-trip: serialize→[[cairn://<pageId>|<label>]] emitted BEFORE wikiLink; parse matches cairn:// BEFORE the wiki rule (invalid pageIds fall through to wiki, never unsafe). GET /api/cairn/{backlinks,search,preview} (owner-scoped, pageId-validated). **Verified prod build (:3210, CAIRN_BASE_URL unset):** PUT cairnLink doc→204; cairn_links row cairn-test-page-xyz (extraction at save); backlinks API→[{docId,title}] (bidirectional); invalid pageId(traversal)→400; unauth→401; preview off-by-default→{url:null} NO external call. extractCairnPageIds direct-tested→["cairn-test-page-xyz"]. biome 0/0, tsc 0, 1065 unit (15 cairn, wiki round-trip intact), build compiles. **WORKFLOW NOTE: implement agent emitted GARBAGE text + committed nothing; review caught "never implemented" (7 blocking), fix agent built the whole feature (e5ab0f8) + restored an incidental search_vector GENERATED drift on both DBs. Controller independently verified git/files/migration/search_vector + ran own gate — agent text untrustworthy, ALWAYS verify real state.** GAP: live Cairn fetch/preview-card content not testable (no Cairn instance — off path + wiring verified). |
| J2 | Calendar embed — read-only iCal iframe | DONE | ☑ | ☑ | Shared embed node (J2+J3, PR pending). One `embed` Tiptap node {provider,url,title}; allowlist decision RE-MADE at render from url (embed-providers.ts resolveProvider/toEmbedUrl, PURE) so a tampered stored url still can't iframe. CRUX: iframe src is ALWAYS an allowlisted https provider URL or NO iframe. EmbedView NodeView sandboxed iframe (allow-scripts allow-same-origin allow-popups, referrerpolicy=no-referrer, loading=lazy) for allowlisted; non-allowlisted→link card (https? only, rel=noopener); non-http scheme→inert text. Calendar provider = Google Calendar embed (bare webcal/.ics→link card, no script-free viewer). parchment:embed md fence round-trip. embed.ts schema has ZERO heavy/client/embed-providers imports (getSchema server-safe — the diagram lesson). **Browser-verified live: allowlisted GSheets→1 iframe host docs.google.com + sandbox attrs; evil.example→link card no iframe; data:XSS→inert "Unsupported link" no iframe.** 1050 unit (15 embed-providers, adversarial), build compiles. Review fixed 2 BLOCKING (link-card raw href allowed data:/exotic→gated https? only; Office test-host typo masked viewer branch). GAP: external content/auth not headless-verifiable; public share viewer renders embeds as link not iframe (deliberate). |
| J3 | Spreadsheet embed — GSheets/Cairn-db/Airtable iframe | DONE | ☑ | ☑ | Same shared embed node as J2 (one impl). Spreadsheet providers: Google Sheets (/edit→pubhtml/widget embed form), Airtable (/embed), Office (view.officeapps.live.com/op/embed). Allowlist host-exact (no substring bypass — lookalike/userinfo-@ rejected). Browser-verified above (GSheets URL→sandboxed iframe on docs.google.com). See J2 notes. |
| J4 | Slack / Discord — share to channel, notify on comment | DONE | ☑ | ☑ | Implemented as preset webhook kinds on the J7 engine (one impl). webhooks.ts formatSlack/formatDiscord shape the message body for those incoming-webhook URLs (no signature header — the URL IS the secret). Presets in WebhooksManager UI: "share to channel" = slack/discord hook on document.published; "notify on comment" = on comment.created. Verified via the J7 end-to-end below (kind routing + dispatch). See J7. |
| J5 | Email-in — per-doc address, SMTP relay replies → comments | DONE | ☑ | ☑ | NO migration — address is a STATELESS unforgeable HMAC. email-in.ts: docInboundAddress = doc.<docId>.<sig>@<INBOUND_EMAIL_DOMAIN> (sig=truncated base32 HMAC-SHA256(INBOUND_EMAIL_SECRET,docId)); parseInboundAddress recomputes + CONSTANT-TIME compares (timingSafeEqualStr); formatInboundComment sanitizes (strip HTML/control, collapseWhitespace on From/Subject single-line fields, cap length). POST /api/email/inbound: X-Inbound-Secret constant-time (401 wrong, 404 unconfigured — doesn't reveal endpoint) → parseInboundAddress (400 forged) → getDocument → createThread(docId, null, body) (external sender = authorId null, provenance in body). GET /api/docs/[id]/inbound-address (owner-scoped, null when unconfigured). Per-doc address shown in CommentsSidebar only when configured. **Verified prod build (:3210, CONFIGURED):** address minted; valid secret+address→200 comment created (From/Subject provenance + body); wrong secret→401; FORGED sig→400 (unforgeable); newline-injection From→collapsed (no forged Subject line); rejected attempts created 0 comments. **Unconfigured:** inbound-address→{null}, endpoint→404. 1111 unit (15 email-in), build compiles. Review fixed 1 BLOCKING (From/Subject newline→provenance-line injection→collapseWhitespace). MINOR: oversized-body guard buffers via req.text() before size check (Content-Length cap present). GAP: live MTA/SMTP delivery not exercised (operator's inbound-parse/Postfix-pipe POSTs to the endpoint; crypto+endpoint+off-path verified); no Message-ID dedupe (counter to stateless design). |
| J6 | GitHub — embed PR/issue with live status | DONE | ☑ | ☑ | githubEmbed node {owner,repo,number,kind} (getSchema-safe) + GithubEmbedView card (live status badge open/closed/merged/draft, loading/unavailable states, always a plain github.com link fallback, title/author as escaped TEXT). integrations/github.ts: parseGithubRef (STRICT anti-SSRF — host must be EXACTLY github.com, owner/repo ^[A-Za-z0-9._-]+$, number ^\d+$ + ≤9 digits), githubApiUrl ALWAYS api.github.com, fetchGithubStatus resilient (network/404/403→null, GITHUB_TOKEN server-only optional, never client-exposed). GET /api/github/status?url= (auth, 400 on invalid/non-github before any fetch, 60s in-mem cache, graceful {unavailable}). parchment:github md fence round-trip. **Verified prod build (:3210, LIVE GitHub fetch):** valid PR→{title,state:merged,author,kind:pr} real card (title with <iframe> escaped, no XSS); non-github host→400; lookalike github.com.evil.com→400; oversized 21-digit number→400; unauth→401. 1080 unit (16 github incl host-spoof+oversized-number), build compiles, no migration. Review 1 finding (0 blocking); controller fixed the 1 MINOR (oversized number→parseInt sci-notation→cap ≤9 digits, ac9c73c + regression test). GAP: live API depends on network/rate-limit (boundary+degradation verified; live path confirmed via the real PR fetch above). |
| J7 | Webhooks — save/publish/comment, HMAC-signed | DONE | ☑ | ☑ | webhooks table (migration 0019, FK owner cascade, events jsonb) APPLIED both DBs. webhooks.ts: signPayload (HMAC-SHA256 sha256=<hex>), buildRequest (generic→X-Parchment-Signature+X-Parchment-Event; slack/discord→formatted body). webhook-dispatch.ts dispatchWebhooks NON-BLOCKING (void deliver per target — never awaits the fetch; deliver has 5s timeout + swallows ALL errors; whole fn try/catch wrapped) at saveDocument(void)/createShare/createThread+addReply chokepoints → a bad webhook can never fail/slow the user action. CRUD /api/webhooks + [id] + [id]/test (sendTestWebhook awaits for UI feedback). Secret CSPRNG, server-only, MASKED in list (secretMask ••••). url http(s)-validated, events⊆3, owner-scoped. WebhooksManager UI in settings/developer + Slack/Discord presets (=J4). **Verified prod build (:3210) end-to-end with a capture server:** create→201 (secret once); list→secret masked; PUT save→204 in 35ms (NON-BLOCKING even with a dead-url hook subscribed); captured outbound POST event=document.saved + X-Parchment-Signature INDEPENDENTLY re-computed HMAC = VALID. 1096 unit (15 webhooks), build compiles. Review 0 findings; controller independently verified non-blocking (void deliver) + real signed delivery. GAP: SSRF-to-internal-host v0.2 hardening (http(s) enforced now); no retry/dead-letter; live Slack/Discord endpoint delivery not asserted (signing+format+dispatch verified). |

## Plan K — Accessibility + i18n / TIER 8 (7)

| ID | Item | Status | Cov | FM | Notes |
|---|---|---|---|---|---|
| K1 | ARIA structure, semantic HTML, alt text required on insert | DONE | ☑ | ☑ | Audit-and-remediate validated by axe. Single <main id="main-content"> per page (demoted FileManager inner <main> panels→div, fixed nested-landmark), <nav aria-label="Primary">, ordered headings, icon-button aria-labels, list semantics. Fixed <ul>-direct-<p> in WebhooksManager + --muted-on-tint contrast in AppearanceSettings. Alt-required-on-insert (B5) confirmed. Controller ran axe e2e → 19/19 routes 0 violations. See K4. |
| K2 | High-contrast theme + OpenDyslexic toggle | DONE | ☑ | ☑ | Extends I1 theme: WorkspaceTheme + highContrast + dyslexicFont booleans (parseTheme legacy-compat ===true/absent→false). globals.css [data-high-contrast=true] AAA palette (light #fff/#000 + dark #000/#fff, accents ≥7:1, layers on scheme) + @font-face OpenDyslexic (self-hosted public/fonts/*.woff2 115KB+120KB valid wOF2, OFL README, NO CDN). Layout data-high-contrast + data-font attrs. AppearanceSettings two toggles persist via /api/settings/theme. **Browser-verified PROD build (:3000): HC→sidebar bg #000 (21:1), data-high-contrast=true; OpenDyslexic→nav+buttons+document text all render OpenDyslexic (font loaded), data-font=dyslexic, persists.** 1116 unit, build compiles. Implement agent emitted GARBAGE+committed nothing (recurring); review caught "not implemented" (15 blocking), fix agent built it all (06ad8e2). **CONTROLLER-FOUND+FIXED (fae6256) — the I1 inline-var lesson STRUCK AGAIN: themeCssVars emits --font-body INLINE on the wrapper, shadowing the [data-font=dyslexic] CSS override → dyslexic font reached prose (direct font-family) but NOT UI chrome; fix = emit OpenDyslexic stack from themeCssVars when dyslexicFont + wrapper applies font-family:var(--font-body). DEV HMR did NOT recompile the server-rendered inline style → only the PROD build confirmed the chrome fix (dev verification was a false-negative).** GAP: none. |
| K3 | Keyboard-only nav — every menu, focus ring, skip-to-content | DONE | ☑ | ☑ | Skip-to-content link (visually-hidden-until-focus, first focusable, →#main-content) in app layout; global :focus-visible ring; menus/dialogs keyboard-operable. Review fixed 2 BLOCKING: Help dialogs now restore focus to the trigger (useFocusTrap restoreRef, fixing the unmount-race where activeElement was already body — WCAG 2.4.3/G15); duplicate <main> removed. MINOR logged: code-block toolbar buttons suppress the focus ring; public pages have #main-content but no skip-link (single-column, acceptable); focus-visible border-radius cosmetic. Controller-verified via axe 19/19 + SSR markup. |
| K4 | axe-core harness — every top-level page a Playwright a11y target | DONE | ☑ | ☑ | Extended a11y.authed.spec (now 15 authed incl new /settings/admin/schedules + /backup) + a11y.public.spec (4: landing/login/setup/invalid-share) = 19 routes, withTags wcag2a/wcag2aa, 0-violation assertion. **Crux infra fix: the e2e DB :5434 had DRIFTED (missing 7 tables + 3 documents columns + collab_state.state text→bytea) → every authed route 500'd (not an a11y fail) — the G9 lesson at the test-DB level; synced :5434→schema.ts (drizzle-kit push + psql), now 20-table parity with :5433.** **Controller independently ran `pnpm test:e2e`: 19/19 passed, 0 WCAG 2 A/AA violations** (incl. /d/[id] editor). 1111 unit, build compiles. GAP: drizzle-kit push emits `"undefined"."bytea"` + aborts → bytea applied via psql (a fresh db:push hits this); e2e DB needs a reproducible CI seed/reset so it can't silently drift again. |
| K5 | i18n via next-intl + RTL (Arabic/Hebrew) | TODO | ☐ | ☐ | |
| K6 | Spell check — browser-native + per-workspace custom dict | TODO | ☐ | ☐ | |
| K7 | Grammar check — LanguageTool (host URL + key UI) | TODO | ☐ | ☐ | |

## Plan L — Release / CI / docs (6)

| ID | Item | Status | Cov | FM | Notes |
|---|---|---|---|---|---|
| L1 | Multi-arch Docker `ghcr.io/jonathanmcohen/parchment:v0.1.0` (amd64 + arm64) | TODO | ☐ | ☐ | |
| L2 | GH Actions release pipeline — `release.yml`, `verify-carry-forward-closed`, tag gated on green e2e+a11y | TODO | ☐ | ☐ | mirror Cairn |
| L3 | `release/v0.1.0` integration branch — per-item PR squash → tag → publish; **keep branch** (no cleanup) | TODO | ☐ | ☐ | user: keep old release branch |
| L4 | README — install, env, commands, upgrade | TODO | ☐ | ☐ | |
| L5 | In-app "What's new in v0.1.0" release notes page | TODO | ☐ | ☐ | |
| L6 | Parchment Guide workspace seed — per-feature page tree + release-notes parent | TODO | ☐ | ☐ | |

---

## Roll-up

| Plan | Items | DONE | GAP | Open |
|---|---|---|---|---|
| A Foundations | 5 | 5 | 0 | 0 |
| B Editor core | 14 | 14 | 0 | 0 |
| C Code block | 7 | 7 | 0 | 0 |
| D Collab | 5 | 5 | 0 | 0 |
| E File manager | 11 | 11 | 0 | 0 |
| F Disk mirror | 6 | 6 | 0 | 0 |
| G Tiers 2–8 | 17 | 17 | 0 | 0 |
| H Export/import | 9 | 9 | 0 | 0 |
| I Settings/ops | 10 | 10 | 0 | 0 |
| J Integrations | 7 | 7 | 0 | 0 |
| K A11y/i18n | 7 | 4 | 0 | 3 |
| L Release/CI | 6 | 0 | 0 | 6 |
| **Total** | **104** | **95** | **0** | **9** |

Shared items (one impl, tracked twice): A4≡I5, A5≡I6, B5↔K1, D3↔F5.

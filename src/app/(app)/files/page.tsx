export default function FilesPage() {
  return (
    <section>
      <h1 className="font-semibold text-2xl tracking-tight">Files</h1>
      <p className="mt-2 text-[var(--muted)]">
        Drive-style file manager. Folders, tags, search, bulk actions — Plan E.
      </p>
    </section>
  )
}

export default function InboxPage() {
  return (
    <section>
      <h1 className="font-semibold text-2xl tracking-tight">Inbox</h1>
      <p className="mt-2 text-[var(--muted)]">Notifications — mentions, comments, shares.</p>
    </section>
  )
}

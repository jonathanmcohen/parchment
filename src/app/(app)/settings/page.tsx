export default function SettingsPage() {
  return (
    <section>
      <h1 className="font-semibold text-2xl tracking-tight">Settings</h1>
      <p className="mt-2 text-[var(--muted)]">
        Account / Workspace / Admin / Developer / Notifications / Security — Plans A3 / I.
      </p>
    </section>
  )
}

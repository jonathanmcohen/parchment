export default function TemplatesPage() {
  return (
    <section>
      <h1 className="font-semibold text-2xl tracking-tight">Templates</h1>
      <p className="mt-2 text-[var(--muted)]">
        Bundled gallery + save-as-template from any doc — Plan G2.
      </p>
    </section>
  )
}
